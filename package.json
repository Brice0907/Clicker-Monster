{
  "name": "clicker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite", 
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build & firebase deploy"
  },
  "devDependencies": {
    "sass": "^1.56.2",
    "vite": "^4.0.0"
  }
}
